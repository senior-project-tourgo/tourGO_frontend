import { View, Text } from 'react-native';

export default function SavedPlacesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-lg font-semibold">Saved Places</Text>
    </View>
  );
}
