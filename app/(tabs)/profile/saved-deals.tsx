import { View, Text } from 'react-native';

export default function SavedDealsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-lg font-semibold">Saved Deals</Text>
    </View>
  );
}
